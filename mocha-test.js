describe("pow", function() {
    it("2 to the power 2 is 4", function() {
      assert.equal(pow(2,2),4); 
    });

    it("4 to the power 4 is 2", function() {
        assert.equal(pow(4,2),16); 
      });  
  
  });